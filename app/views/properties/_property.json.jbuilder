json.extract! property, :id, :property_category_id, :name, :street, :number, :complement, :neighborhood, :cep, :city_id, :latitude, :longitude, :created_at, :updated_at
json.url property_url(property, format: :json)